name: Test

on: 
  pull_request:
    branches: 
      - master

jobs:
  whatever:
    name: trying to create message
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: step1
      run: a='grep -q "put" README.md'
    - name: step1-2
      run: grep -q "put" README.md
    - name: step2
      run: b='grep -q "longRunningOperation:true" README.md'
    - name: step2-2
      run: grep -q "longRunningOperation:true" README.md
    - name: step3
      run: a==b
